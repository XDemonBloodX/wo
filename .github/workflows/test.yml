name: Test

on:
  push:
      branches:
             - dev
             - master
jobs:
 hello-world:
   name: My hello world workflow
   runs-on: ubuntu-20.04
   
   steps:
     - name: Say hello world
       shell: bash
       run: echo welcome git
